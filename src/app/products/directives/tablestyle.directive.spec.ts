import { TablestyleDirective } from './tablestyle.directive';

describe('TablestyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TablestyleDirective();
    expect(directive).toBeTruthy();
  });
});
